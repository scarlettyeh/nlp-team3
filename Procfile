# web process is going to run flask app 
# environment variable = web.py 
# run python & flask 
# run couple of parameters - host and port 
web: FLASK_APP=web.py python -m flask run --host=0.0.0.0 --port=$PORT
